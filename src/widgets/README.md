# widgets (FSD layer)

Self-contained, composite UI blocks that combine `features` and `entities` into a
reusable section (e.g. header, sidebar, post card with actions). Widgets may use
features/entities/shared; they must not import from `views` or other widgets.
